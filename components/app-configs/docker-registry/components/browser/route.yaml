---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: browser
spec:
  port:
    targetPort: 8080
  tls:
    insecureEdgeTerminationPolicy: Allow
    termination: edge
  to:
    kind: Service
    name: registry-v2
    weight: 100
  wildcardPolicy: None
