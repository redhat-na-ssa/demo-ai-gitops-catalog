apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: adhoc-ops

resources:
  - adhoc-pod.yaml
  - admin-rolebinding.yaml
  - namespace.yaml
  - terminal-role.yaml
  - terminal-rolebinding.yaml
