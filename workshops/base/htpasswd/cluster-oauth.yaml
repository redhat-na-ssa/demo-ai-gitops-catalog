apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
spec:
  identityProviders:
    # - htpasswd:
    #     fileData:
    #       name: htpasswd-local
    #   mappingMethod: claim
    #   name: Admins
    #   type: HTPasswd
    - htpasswd:
        fileData:
          name: htpasswd-workshop
      mappingMethod: claim
      name: Workshop Users
      type: HTPasswd
