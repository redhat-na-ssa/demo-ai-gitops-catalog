apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: opencv-notebook
spec:
  source:
    type: Git
    git:
      uri: https://github.com/redhat-na-ssa/demo-argocd-gitops.git
    contextDir: components/demos/instance/custom-notebooks/opencv
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Containerfile
  output:
    to:
      kind: ImageStreamTag
      name: opencv-notebook:latest
      namespace: custom-notebooks
  triggers:
    - type: ImageChange
    - type: ConfigChange
