---
apiVersion: v1
kind: Service
metadata:
  name: registry-v2
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  ports:
  - name: 5000-tcp
    port: 5000
    targetPort: 5000
  selector:
    deploy: registry-v2
  sessionAffinity: None
  type: ClusterIP
