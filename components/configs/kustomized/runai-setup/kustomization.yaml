apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
- includeSelectors: false
  pairs:
    kubernetes.io/part-of: runai

resources:
- namespace.yaml
# - runai-gcr-secret.yaml
# - sandbox-ns.yaml
