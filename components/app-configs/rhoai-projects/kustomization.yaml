apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
- includeSelectors: false
  pairs:
    demo: ai-project

resources:
  - group
  - individual
  - kserve
  - modelmesh
  - open
