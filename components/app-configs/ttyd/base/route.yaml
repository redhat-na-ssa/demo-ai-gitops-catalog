apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: console
spec:
  port:
    targetPort: 8080-tcp
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: ttyd
    weight: 100
  wildcardPolicy: None
