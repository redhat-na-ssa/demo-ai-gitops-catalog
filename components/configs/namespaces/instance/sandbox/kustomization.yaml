apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - sandbox-edit-rolebinding.yaml
  - sandbox-limit-range.yaml
  - sandbox-namespace.yaml
  - sandbox-quota-compute.yaml
  - sandbox-quota-storage.yaml
