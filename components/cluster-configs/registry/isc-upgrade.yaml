kind: ImageSetConfiguration
apiVersion: mirror.openshift.io/v2alpha1
mirror:
  platform:
    channels:
    - name: stable-4.18
      minVersion: 4.18.22
      maxVersion: 4.18.22
    - name: stable-4.19
      minVersion: 4.19.10
      maxVersion: 4.19.10
    graph: true
  operators:
    - catalog: registry.redhat.io/redhat/redhat-operator-index:v4.18
      packages:
        - name: local-storage-operator
        - name: web-terminal
        - name: devworkspace-operator-fast-redhat-operators-openshift-marketplace
    - catalog: registry.redhat.io/redhat/redhat-operator-index:v4.19
      packages:
        - name: local-storage-operator
        - name: web-terminal
        - name: devworkspace-operator-fast-redhat-operators-openshift-marketplace
  additionalImages:
    - name: ghcr.io/redhat-na-ssa/web-terminal-tooling:latest
  helm:
    repositories:
      - name: cosigned
        url: https://sigstore.github.io/helm-charts
        charts:
          - name: cosigned
            version: 0.1.23

# oc-mirror -c ./isc.yaml --workspace file:///${PWD}/scratch/oc-mirror docker://registry:5000 --v2 --image-timeout 60m
