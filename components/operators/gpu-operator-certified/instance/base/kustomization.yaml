apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  kustomize: gpu-operator

namespace: nvidia-gpu-operator

resources:
  - cluster-policy.yaml
  - device-plugin-config.yaml
  - setup-job.yaml
