kind: ConfigMap
apiVersion: v1
metadata:
  name: ca-bundle
  namespace: demo-otel-collector
  annotations:
    service.beta.openshift.io/inject-cabundle: "true"
  # label:
  #   config.openshift.io/inject-trusted-cabundle: 'true'
