apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

namespace: nvidia-gpu-operator

resources:
  - job.yaml

configMapGenerator:
  - name: scripts
    files:
      - ../../../../../../scripts/library/ocp.sh
      - job.sh
