---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: enable-gpu-console-plugin
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: enable-gpu-console-plugin
rules:
  - apiGroups: ["operator.openshift.io"]
    resources:
      - consoles
    verbs:
      - get
      - list
      - patch
      - label
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: enable-gpu-console-plugin
subjects:
  - kind: ServiceAccount
    name: enable-gpu-console-plugin
    namespace: openshift-storage
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: enable-gpu-console-plugin
---
apiVersion: batch/v1
kind: Job
metadata:
  name: enable-gpu-console-plugin
  generateName: enable-gpu-console-plugin-
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  template:
    spec:
      containers:
        - name: labeler
          image: registry.redhat.io/openshift4/ose-cli
          env:
            - name: PLUGIN_NAME
              value: 'console-plugin-nvidia-gpu'
          command:
            - /bin/bash
            - -c
            - /scripts/job.sh
          volumeMounts:
            - name: scripts
              mountPath: /scripts
      volumes:
        - name: scripts
          configMap:
            name: scripts
            defaultMode: 0755
      restartPolicy: Never
      serviceAccount: enable-gpu-console-plugin
      serviceAccountName: enable-gpu-console-plugin
  backoffLimit: 4
