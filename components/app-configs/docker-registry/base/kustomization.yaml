apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
- includeSelectors: true
  pairs:
    app: registry-v2

resources:
- config.yaml
- deployment.yaml
- pvc.yaml
- route.yaml
- service.yaml
