apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ds-open-project

commonLabels:
  opendatahub.io/dashboard: 'true'

resources:
  - admin-rolebinding.yaml
  - edit-rolebinding.yaml
  - limit-range.yaml
  - namespace.yaml
  - quota.yaml
